'use client';

import { StageId } from '@/src/contexts/NavigationContext';

import StoryStage from './ui/StoryStage';

export default function DishonestSelfOwnership() {
  return <StoryStage stage={StageId.DishonestSelfOwnership} />;
}
